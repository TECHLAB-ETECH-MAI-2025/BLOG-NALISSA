{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="my-5 d-flex justify-content-center">
    <div style="max-width: 800px; width: 100%; padding: 0 15px;">
        <h1 class="text-theme">Category</h1>
            <div class="mb-4 d-flex justify-content-end">
            <a href="{{ path('category_new') }}" class="btn btn-theme">
                <i class="bi bi-plus-lg"></i>+ Ajouter un Categorie
            </a>
        </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}" class="btn btn-theme">← Retour aux categories</a>
<<<<<<< HEAD
    {% if is_granted('ROLE_ADMIN') %}
=======
>>>>>>> 0bcbf57 (chore(cleanup): ajustements styles, vues, fixtures)

    
    <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-warning me-2">Modifier</a>
    <form action="{{ path('category_delete', {id: category.id}) }}" method="POST" class="d-inline" onsubmit="return confirm('Voulez-vous vraiment supprimer cet categorie ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete-category' ~ category.id) }}">
        <button type="submit" class="btn btn-danger">Supprimer</button>
    </form>
    {% endif %}



    </div>
   
    

</div>
<div class="container mt-3">
    {% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label == 'danger' ? 'danger' : (label == 'success' ? 'success' : 'info') }} alert-dismissible fade show mt-4" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
        </div>
    {% endfor %}
{% endfor %}

</div>

{% endblock %}
