{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center mb-4">
        <h1 class="display-5 fw-bold">Bienvenue sur notre application</h1>
        <p class="lead">
            Cette application propose un système d'authentification complet avec Symfony.
        </p>
        <p>
            Vous pouvez vous inscrire, vous connecter, gérer votre profil et bien plus encore.
        </p>
    </div>

    <div class="text-center mb-5">
        {% if not app.user %}
            <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg me-3">
                <i class="bi bi-person-plus"></i> S'inscrire
            </a>
            <a href="{{ path('security_login') }}" class="btn btn-outline-primary btn-lg">
                <i class="bi bi-box-arrow-in-right"></i> Se connecter
            </a>
        {% else %}
            <a href="{{ path('app_profile') }}" class="btn btn-success btn-lg me-3">
                <i class="bi bi-person-circle"></i> Mon profil
            </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin') }}" class="btn btn-warning btn-lg">
                    <i class="bi bi-shield-lock"></i> Administration
                </a>
            {% endif %}
        {% endif %}
    </div>

    <div class="row text-center">
        <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-lock"></i> Authentification sécurisée</h5>
                    <p class="card-text">Système complet avec inscription, connexion et gestion des rôles.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-envelope-check"></i> Vérification par email</h5>
                    <p class="card-text">Vérifiez votre adresse email et réinitialisez votre mot de passe en toute sécurité.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-people"></i> Gestion des utilisateurs</h5>
                    <p class="card-text">Interface d'administration pour gérer les utilisateurs et leurs permissions.</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
